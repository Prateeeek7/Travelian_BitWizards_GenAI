# Legacy Streamlit dependencies (kept for reference)
# streamlit==1.32.2
# langchain_google_genai==0.0.11
# google-generativeai>=0.4.1,<0.5.0
# pandas==2.1.4
# geopy==2.4.1
# pydeck==0.8.0

# Current FastAPI + React Architecture
# Backend dependencies are in backend/requirements.txt
# Frontend dependencies are in frontend/package.json

# Optional dependencies for MongoDB integration
pymongo>=4.3.3  # Optional - for MongoDB integration
openai>=1.3.0   # Optional - for vector embeddings